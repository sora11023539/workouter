<% provide(:title, 'ユーザーを探す') %>

<article class="users inner">
  <h1>ユーザーを探す</h1>
  <h4 class="sub-title">条件設定</h4>

  <div class="search-form">
    <%= render 'search' %>
  </div>

  <ul>
    <% @user.each do |user| %>
      <%= render user %>
    <% end %>
  </ul>
</article>
